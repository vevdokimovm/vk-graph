# CHANGELOG — `vk-graph`

## [1.2.6] — 2026-08-29 — README приведён к стандарту 98 (PATCH)

### README приведён к стандарту `98-readme-standard.md`

Стандарт заведён 29.08.2026 по требованию владельца: *«сделай стандарт README
на основе того, как он сделан у `personal-finance-dss` — там самый подробный —
и сделай у всех других по этому стандарту подробный README»*.

🔴 **Прямо копировать образец нельзя**, и это главное в стандарте: у базы
знаний нет API и быстрого старта. Замер подтвердил различие — `product`
в среднем **165 строк**, `satellite` **69**. Разница не в небрежности:
у продукта больше того, что надо рассказать.

Поэтому общий скелет из семи разделов + разделы по классу репы.

**Что добавлено сборщиком `rebuild_readme.py`** — всё **из написанного
человеком**, ничего не сочинено:

- **«С чего начать»** — только существующие файлы, в порядке чтения;
- **«Что здесь можно узнать»** — из **заголовков документов**: заголовок
  и есть вопрос, на который документ отвечает;
- **«Границы — чего здесь НЕТ»** — предложения `MANIFEST.md` §1 с отрицанием;
- **таблица структуры** — столбец «что внутри по существу» берётся
  из `MANIFEST.md` §3, иначе из заголовков файлов, иначе пишется честно,
  что описания нет.

Прежняя таблица «Repository map» с **пустым столбцом Contents** снята —
это и была та «херня с таблицей», о которой говорил владелец.

**По системе:** медиана README **56 → 77 строк**, у баз знаний 61 → 84.

## [1.2.5] — 2026-08-28 — Закрытые задачи вынесены в историю (PATCH)

Закрытые пункты вынесены в `ROADMAP_HISTORY.md`/`TASKS_HISTORY.md` скриптом
`base-repo/scripts/archive_closed_tasks.py`. Рабочие файлы держат только
открытое — требование владельца 28.08.2026 («чисть выполненные в логи, не
оставляй их внутри»). Формулировки перенесены как есть, вместе с обоснованием
снятия. Опустевшие секции свёрнуты в строку-пойнтер, а не удалены.

Накопление найдено хуком `.claude/hooks/task-hygiene.sh` (`base-repo` [3.100.0]),
он же подтвердил чистоту после: 0 реп с накоплением против 8 до.

## [1.2.4] — 2026-08-28 — README: убран служебный блок «Структура» (PATCH)

Служебный блок «Структура» (внутренний артефакт `base-repo`) протёк в README
публичной репы. Убран. Заодно исправлено `.repo-meta`: `private=true` при
факте `PUBLIC` на GitHub.

## [1.2.3] — 2026-08-28 — DRIFT 7 → 2: README STATUS добавлен, гейт-фиксы в base-repo (PATCH)

Гейт `revision_check.py` не учитывал `_base/`-отсутствие у публичных реп
(`base-repo` [3.84.0], `PIT-153`) — эта репа выиграла без правки контента:
DRIFT 7 → 3. Плюс `README.md` получил блок `<!-- STATUS -->` (не было вовсе).
Остаток — 2, структурно неприменимый PIT-091 (нет своего PIT/SYN-реестра).

## [1.2.2] — 2026-08-28 — Плейсхолдер ROADMAP закрыт по факту (PATCH)

`ROADMAP.md` с 22.08.2026 нёс пустой плейсхолдер «определить направление репы»,
хотя направление уже определено и исполнено (v1.2.1, слоистый бэкенд, три
формата выгрузки, `NOTES.md`) — файл заведён пустым до наполнения и не
обновлён вслед за фактом. Закрыто.

## [1.2.1] — 2026-08-26 — Отчёт о ревизии добавлен (reports/revision-2026-08-26.md) (PATCH)

По заказу владельца — каждой ревизуемой репе отдельный отчёт по шаблону finpilot.

## [1.2.0] — 2026-08-25 — Манифест: ревизия — дублей нет, публичная репа с уже встроенной защитой персональных данных (MINOR)

Прочитано целиком (21 файл). Дублей нет. Написан MANIFEST.md.

Формат: [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/), версии по [SemVer](https://semver.org/lang/ru/).

## [1.1.0] — 2026-08-15 — Разбор собственного кода (MINOR)

- Добавлен `NOTES.md`: что в проекте наивного и как я написал бы это сейчас.
  Материал для собеседования — честный разбор слабых мест своего кода
  показывает рост нагляднее, чем сам код.

## [1.0.1] — 2026-08-15 — Метаданные репозитория (PATCH)

- Добавлен `.repo-meta` с описанием и топиками. В 1.0.0 его не было, из-за чего
  `deploy.sh` поставил описанием имя репы и не проставил ни одного топика.

## [1.0.0] — 2026-08-15 — Публичное зеркало заведено (MINOR)

Витрина по ADR-009: каждый показываемый проект — своя публичная репа, зеркало
приватного ядра `it-base/vk-graph`.

- Перенесено по белому списку: 13 файл(ов)
- README переписан под публичный просмотр: чем показателен, что внутри, стек
- Секрет-скан пройден, `.env` и реальные выгрузки не переносятся
- Персональные данные третьих лиц оставлены в приватном ядре
