# vk-graph

<!-- STATUS -->
> **Сейчас:** `v1.2.7` · 2026-08-29 · Пересборка: архив содержал устаревший канон
<!-- /STATUS -->

3D-визуализация графа друзей ВКонтакте: FastAPI-бэкенд, интерактивный фронт, экспорт в JSON и GraphML

**Стек:** `Python 3.12` · `FastAPI` · `Pydantic v2` · `VK API` · `Three.js` · `Docker`

## Что это и зачем

3D-визуализация графа друзей ВКонтакте: FastAPI-бэкенд, интерактивный Three.js-фронт,
экспорт в JSON/GraphML/PNG. Публичное зеркало приватного ядра — белый список на перенос,
секрет-сканер перед пушем, `exports/` (реальные данные третьих лиц: имена, ID, фото
756 человек) в зеркало не входит и в этой локальной копии физически отсутствует
(в `.gitignore`, не создана). Одновременно портфолио-проект под найм — `NOTES.md`
разбирает собственный код без скидок для собеседований.

_Перенесено из [`MANIFEST.md`](MANIFEST.md) §1 — единственного места, где это написано человеком._

## Чем показателен

Профиль junior Python backend в чистом виде: асинхронный REST, работа с внешним API и его лимитами, разделение слоёв, свой фронт поверх.

## Что внутри

- **Слоистый бэкенд** — `api` / `core` / `schemas` разведены: роутер не знает про VK, клиент не знает про HTTP, схемы не знают ни про то, ни про другое
- **Два режима обхода** — только друзья или друзья друзей; второй режим упирается в лимиты VK API, и это решается батчингом в `vk_client`
- **Три формата выгрузки** — JSON для своего фронта, GraphML для Gephi, PNG для отчёта
- **Токен только из окружения** — `.env.example` пустой, в коде хардкода нет

## Что не попало в публичную версию

**Папка `exports/` в зеркало не входит.** В приватном ядре там лежат реальные выгрузки: 756 узлов с именами, идентификаторами и ссылками на фотографии живых людей и 11 448 связей между ними. Это персональные данные третьих лиц, и в публичной репе им места нет ни в каком виде. Формат выгрузок виден по коду `backend/core/exporter.py`.

## Разбор собственного кода

[`NOTES.md`](./NOTES.md) — что здесь наивного и как я написал бы это сейчас: слабые места, найденные в своём же проекте, и конкретные решения вместо них.

## Как это опубликовано

Публичное зеркало приватного ядра. Ядро живёт в закрытом репозитории,
сюда переносится **только то, что явно внесено в белый список** — не «всё, кроме
исключений»: чёрные списки протекают, и одного забытого шаблона хватает,
чтобы уехало лишнее. Публикация идёт скриптом, перед пушем прогоняется
секрет-сканер. Решение и его основания — ADR-009 в системе репозиториев автора.

---

**Автор:** Василий Евдокимов · [@vevdokimovm](https://github.com/vevdokimovm) · vevdokimovm@gmail.com

## Границы — чего здесь НЕТ

- Публичное зеркало приватного ядра — белый список на перенос, секрет-сканер перед пушем, `exports/` (реальные данные третьих лиц: имена, ID, фото 756 человек) в зеркало не входит и в этой локальной копии физически отсутствует (в `.gitignore`, не создана).

_Перенесено из [`MANIFEST.md`](MANIFEST.md) §1._

## Структура

| Каталог | Что внутри |
|---|---|
| `frontend/` | Клиентская часть: `index.html`, отрисовка графа в браузере |

## Структура

<!-- STRUCTURE:AUTO:START -->
| Папка | Файлов | Что внутри по существу |
|---|---|---|
| [`backend/`](backend/) | 9 | _описания нет — ни в `MANIFEST.md` §3, ни в заголовках файлов_ |
| [`frontend/`](frontend/) | 1 | `index.html` — интерактивный 3D-фронт |
| [`reports/`](reports/) | 3 | заметка кампании скриншотов (02.05.2026): промежуточный снимок рендера (753/10727) против финальных чисел README (756/11448) — сверено, объяснено как более ранний срез того же обхода |

_Столбец «что внутри» собран из `MANIFEST.md` §3, где он есть, иначе из заголовков самих файлов. Ничего не сочинено: где описания нет — так и написано._
<!-- STRUCTURE:AUTO:END -->
